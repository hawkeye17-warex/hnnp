{
  "name": "@hnnp/cloud-api",
  "version": "0.1.0",
  "private": true,
  "description": "Workspace wrapper for the HNNP Cloud API backend service implemented in ./backend.",
  "scripts": {
    "postinstall": "npm --prefix ../backend install --include=dev --production=false",
    "dev": "npm --prefix ../backend run dev",
    "build": "npm --prefix ../backend run build",
    "start": "npm --prefix ../backend run start",
    "test": "npm --prefix ../backend run test"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.83.0",
    "dotenv": "^16.4.5"
  }
}
